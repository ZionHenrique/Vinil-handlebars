<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Adicionar Vinil</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; max-width: 600px; }
        h1 { color: #333; }
        form { margin-top: 20px; }
        label { display: block; margin-top: 15px; font-weight: bold; }
        input, select { width: 100%; padding: 8px; margin-top: 5px; box-sizing: border-box; }
        select[multiple] { height: 100px; }
        small { display: block; color: #666; font-size: 12px; margin-top: 5px; }
        button { background-color: #28a745; color: white; padding: 10px 20px; border: none; cursor: pointer; margin-top: 20px; }
        button:hover { background-color: #218838; }
        a { color: #0066cc; text-decoration: none; margin-right: 10px; }
        a:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <h1>Adicionar Novo Vinil</h1>
    <form action="/vinis" method="POST">
        <label for="nome">Nome do Vinil:</label>
        <input type="text" id="nome" name="nome" placeholder="Nome do vinil" required>
        
        <label for="artista">Artista:</label>
        <input type="text" id="artista" name="artista" placeholder="Nome do artista" required>
        
        <label for="preco">Preço:</label>
        <input type="number" id="preco" name="preco" step="0.01" placeholder="0.00" min="0" required>
        
        <label for="ano">Ano:</label>
        <input type="number" id="ano" name="ano" placeholder="2024" min="1900" max="2100" required>
        
        <label for="quant">Quantidade:</label>
        <input type="number" id="quant" name="quant" placeholder="0" min="0" required>
        
        <label for="genero">Gênero(s):</label>
        <select id="genero" name="genero" multiple>
            {{#each generos}}
            <option value="{{nome}}">{{nome}}</option>
            {{/each}}
        </select>
        <small>Mantenha Ctrl (Windows) ou Cmd (Mac) pressionado para selecionar múltiplos gêneros</small>
        
        <button type="submit">Adicionar Vinil</button>
    </form>
    <p>
        <a href="/vinis">← Voltar para Lista</a>
        <a href="/">Voltar para Home</a>
    </p>
</body>
</html>