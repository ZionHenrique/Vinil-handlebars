<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{genero.nome}} - Vinil Store</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header class="header">
        <div class="header-content">
            <a href="/" class="logo">Vinil Store</a>
            <nav class="nav">
                <a href="/">Home</a>
                <a href="/vinis">Vinis</a>
                <a href="/perfis">Clientes</a>
                <a href="/compras">Compras</a>
                <a href="/generos">Gêneros</a>
            </nav>
        </div>
    </header>

    <div class="container">
        <div class="card">
            <h1 class="card-title">{{genero.nome}}</h1>
            
            <div style="margin-top: 30px;">
                <div style="margin-bottom: 20px;">
                    <strong style="color: var(--primary-color); font-size: 18px;">ID:</strong>
                    <span style="font-size: 18px; margin-left: 10px;">{{genero.id}}</span>
                </div>
                
                <div style="margin-bottom: 20px;">
                    <strong style="color: var(--primary-color); font-size: 18px;">Nome:</strong>
                    <span style="font-size: 18px; margin-left: 10px;">{{genero.nome}}</span>
                </div>
                
                <div style="margin-bottom: 30px;">
                    <strong style="color: var(--primary-color); font-size: 18px; display: block; margin-bottom: 10px;">Descrição:</strong>
                    <p style="font-size: 16px; line-height: 1.6; color: #666; padding: 15px; background: #f8f9fa; border-radius: 8px;">{{genero.descricao}}</p>
                </div>
            </div>
            
            <div class="form-actions">
                <a href="/generos/{{genero.id}}/edit" class="btn btn-primary">Editar Gênero</a>
                <form action="/generos/{{genero.id}}/delete" method="POST" style="display: inline;">
                    <button type="submit" class="btn btn-danger" onclick="return confirm('Tem certeza que deseja excluir este gênero?')">Excluir</button>
                </form>
                <a href="/generos" class="btn btn-secondary">Voltar para Lista</a>
            </div>
        </div>
    </div>
</body>
</html>
