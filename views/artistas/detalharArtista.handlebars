<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{artista.nome}} - Vinil Store</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header class="header">
        <div class="header-content">
            <a href="/" class="logo">Vinil Store</a>
            <nav class="nav">
                <a href="/">Home</a>
                <a href="/vinis">Vinis</a>
                <a href="/perfis">Clientes</a>
                <a href="/compras">Compras</a>
                <a href="/artistas">Artistas</a>
                <a href="/musicas">Músicas</a>
                <a href="/generos">Gêneros</a>
            </nav>
        </div>
    </header>

    <div class="container">
        <div class="card">
            <h1 class="card-title">{{artista.nome}}</h1>
            
            <div style="margin-top: 30px;">
                <div style="margin-bottom: 20px;">
                    <strong style="color: var(--primary-color); font-size: 18px;">ID:</strong>
                    <span style="font-size: 18px; margin-left: 10px;">{{artista.id}}</span>
                </div>
                
                {{#if artista.pais}}
                <div style="margin-bottom: 20px;">
                    <strong style="color: var(--primary-color); font-size: 18px;">País:</strong>
                    <span style="font-size: 18px; margin-left: 10px;">{{artista.pais}}</span>
                </div>
                {{/if}}
                
                {{#if artista.ano_inicio}}
                <div style="margin-bottom: 20px;">
                    <strong style="color: var(--primary-color); font-size: 18px;">Ano de Início:</strong>
                    <span style="font-size: 18px; margin-left: 10px;">{{artista.ano_inicio}}</span>
                </div>
                {{/if}}
                
                {{#if artista.biografia}}
                <div style="margin-bottom: 30px;">
                    <strong style="color: var(--primary-color); font-size: 18px; display: block; margin-bottom: 10px;">Biografia:</strong>
                    <p style="font-size: 16px; line-height: 1.6; color: #666; padding: 15px; background: #f8f9fa; border-radius: 8px;">{{artista.biografia}}</p>
                </div>
                {{/if}}
            </div>
            
            {{! Associação bidirecional: Artistas ↔ Músicas (1:N) }}
            <div style="margin-top: 40px;">
                <h2 style="color: var(--primary-color); margin-bottom: 20px; border-bottom: 2px solid var(--accent-color); padding-bottom: 10px;">Músicas do Artista</h2>
                {{#if artista.musicas}}
                    {{#if artista.musicas.length}}
                        <div style="overflow-x: auto;">
                            <table style="width: 100%; border-collapse: collapse; margin-top: 20px;">
                                <thead>
                                    <tr style="background: var(--primary-color); color: white;">
                                        <th style="padding: 12px; text-align: left;">ID</th>
                                        <th style="padding: 12px; text-align: left;">Nome da Música</th>
                                        <th style="padding: 12px; text-align: left;">Duração</th>
                                        <th style="padding: 12px; text-align: center;">Ações</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {{#each artista.musicas}}
                                    <tr style="border-bottom: 1px solid #ddd;">
                                        <td style="padding: 12px;">{{this.id}}</td>
                                        <td style="padding: 12px;">{{this.nome}}</td>
                                        <td style="padding: 12px;">{{this.duracao}}</td>
                                        <td style="padding: 12px; text-align: center;">
                                            <a href="/musicas/{{this.id}}" class="btn btn-secondary" style="padding: 5px 10px; font-size: 14px;">Ver</a>
                                        </td>
                                    </tr>
                                    {{/each}}
                                </tbody>
                            </table>
                        </div>
                    {{else}}
                        <p style="color: #666; font-style: italic; margin-top: 20px;">Este artista ainda não tem músicas cadastradas.</p>
                    {{/if}}
                {{else}}
                    <p style="color: #666; font-style: italic; margin-top: 20px;">Este artista ainda não tem músicas cadastradas.</p>
                {{/if}}
            </div>
            
            <div class="form-actions">
                <a href="/artistas/{{artista.id}}/edit" class="btn btn-primary">Editar Artista</a>
                <form action="/artistas/{{artista.id}}/delete" method="POST" style="display: inline;">
                    <button type="submit" class="btn btn-danger" onclick="return confirm('Tem certeza que deseja excluir este artista?')">Excluir</button>
                </form>
                <a href="/artistas" class="btn btn-secondary">Voltar para Lista</a>
            </div>
        </div>
    </div>
</body>
</html>
