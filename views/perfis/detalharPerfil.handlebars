<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{perfil.nome}} - Vinil Store</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header class="header">
        <div class="header-content">
            <a href="/" class="logo">Vinil Store</a>
            <nav class="nav">
                <a href="/">Home</a>
                <a href="/vinis">Vinis</a>
                <a href="/perfis">Clientes</a>
                <a href="/compras">Compras</a>
                <a href="/generos">Gêneros</a>
                <a href="/distribuidoras">Distribuidoras</a>
            </nav>
        </div>
    </header>

    <div class="container">
        <div class="card">
            <h1 class="card-title">{{perfil.nome}}</h1>
            
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 30px; margin-top: 30px;">
                <div>
                    <h2 style="color: var(--primary-color); margin-bottom: 20px; border-bottom: 2px solid var(--accent-color); padding-bottom: 10px;">Informações Pessoais</h2>
                    <div style="margin-bottom: 15px;">
                        <strong style="color: var(--primary-color);">ID:</strong>
                        <span style="font-size: 18px; margin-left: 10px;">{{perfil.id}}</span>
                    </div>
                    <div style="margin-bottom: 15px;">
                        <strong style="color: var(--primary-color);">Nome:</strong>
                        <span style="font-size: 18px; margin-left: 10px;">{{perfil.nome}}</span>
                    </div>
                    <div style="margin-bottom: 15px;">
                        <strong style="color: var(--primary-color);">Email:</strong>
                        <span style="font-size: 18px; margin-left: 10px;">{{perfil.email}}</span>
                    </div>
                </div>
                
                <div>
                    <h2 style="color: var(--primary-color); margin-bottom: 20px; border-bottom: 2px solid var(--accent-color); padding-bottom: 10px;">Contato</h2>
                    <div style="margin-bottom: 15px;">
                        <strong style="color: var(--primary-color);">Telefone:</strong>
                        <span style="font-size: 18px; margin-left: 10px;">{{perfil.telefone}}</span>
                    </div>
                    <div style="margin-bottom: 15px;">
                        <strong style="color: var(--primary-color);">Endereço:</strong>
                        <span style="font-size: 18px; margin-left: 10px;">{{perfil.endereco}}</span>
                    </div>
                </div>
            </div>
            
            {{! Associação bidirecional: Perfis ↔ Compras (1:N) }}
            <div style="margin-top: 40px;">
                <h2 style="color: var(--primary-color); margin-bottom: 20px; border-bottom: 2px solid var(--accent-color); padding-bottom: 10px;">Compras Realizadas</h2>
                {{#if perfil.compras}}
                    {{#if perfil.compras.length}}
                        <div style="overflow-x: auto;">
                            <table style="width: 100%; border-collapse: collapse; margin-top: 20px;">
                                <thead>
                                    <tr style="background: var(--primary-color); color: white;">
                                        <th style="padding: 12px; text-align: left;">ID</th>
                                        <th style="padding: 12px; text-align: left;">Vinil</th>
                                        <th style="padding: 12px; text-align: left;">Artista</th>
                                        <th style="padding: 12px; text-align: center;">Quantidade</th>
                                        <th style="padding: 12px; text-align: right;">Total</th>
                                        <th style="padding: 12px; text-align: center;">Data</th>
                                        <th style="padding: 12px; text-align: center;">Ações</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {{#each perfil.compras}}
                                    <tr style="border-bottom: 1px solid #ddd;">
                                        <td style="padding: 12px;">{{this.id}}</td>
                                        <td style="padding: 12px;">{{this.vinilNome}}</td>
                                        <td style="padding: 12px;">{{this.vinilArtista}}</td>
                                        <td style="padding: 12px; text-align: center;">{{this.quantidade}}</td>
                                        <td style="padding: 12px; text-align: right;">R$ {{this.total}}</td>
                                        <td style="padding: 12px; text-align: center;">{{this.data}}</td>
                                        <td style="padding: 12px; text-align: center;">
                                            <a href="/compras/{{this.id}}" class="btn btn-secondary" style="padding: 5px 10px; font-size: 14px;">Ver</a>
                                        </td>
                                    </tr>
                                    {{/each}}
                                </tbody>
                            </table>
                        </div>
                    {{else}}
                        <p style="color: #666; font-style: italic; margin-top: 20px;">Este cliente ainda não realizou nenhuma compra.</p>
                    {{/if}}
                {{else}}
                    <p style="color: #666; font-style: italic; margin-top: 20px;">Este cliente ainda não realizou nenhuma compra.</p>
                {{/if}}
            </div>
            
            <div class="form-actions" style="margin-top: 30px;">
                <a href="/perfis/{{perfil.id}}/edit" class="btn btn-primary">Editar Cliente</a>
                <form action="/perfis/{{perfil.id}}/delete" method="POST" style="display: inline;">
                    <button type="submit" class="btn btn-danger" onclick="return confirm('Tem certeza que deseja excluir este cliente?')">Excluir</button>
                </form>
                <a href="/perfis" class="btn btn-secondary">Voltar para Lista</a>
            </div>
        </div>
    </div>
</body>
</html>

